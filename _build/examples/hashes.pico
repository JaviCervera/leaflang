hash = {"msg": "Hello"}
Print(hash["msg"]$)
hash["otherMsg"] = "Bye"
Print(hash["otherMsg"]$)

list = ["One", "Two", "Three"]
list[Size(list)] = "Last"
Print(Size(list)$)
for i = 0 to Size(list) - 1 do
    Print(list[i]$)
end

hash = {
    "foo": {
        "bar": "A message"
    }
}

Print(hash["foo"]["bar"]$)

hash = {}
hash["foo"] = {
    "baz": "Another message"
}

Print(hash["foo"]["baz"]$)
